name: MyCI
on: [push]
jobs:
  dependencies:
    runs-on: ubuntu-latest
    steps:
      - name: dependencies
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm i

    
      
